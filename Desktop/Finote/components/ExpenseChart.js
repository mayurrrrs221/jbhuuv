import React from 'react';
import { View, Text } from 'react-native';

// Placeholder: Replace with victory-native/any chart lib usage for real charts
export default function ExpenseChart({ data }) {
  return (
    <View style={{ padding: 25, alignItems: 'center' }}>
      <Text style={{ fontSize: 18, fontWeight: 'bold' }}>[Expense Chart Here]</Text>
      {/* Insert charting library component here as needed */}
    </View>
  );
}
